# Log 2026-08-18 17:47 — Header con grid por áreas (título a la izquierda en web)

Resultado: **éxito** — 235 tests, lint 0 errores, build OK, CI verde.

## Notificación Telegram
🌍 La Idea — título pegado a la izquierda en web

El título seguía quedando casi centrado en escritorio. El problema era el flexbox: con 3 elementos, `justify-between` siempre centra el del medio.
• Lo cambié por **CSS Grid con áreas**: en web ahora es una fila `☰ · La Idea · botones`, con el título anclado a la izquierda justo al lado de la hamburguesa.
• En móvil no cambia nada: título arriba, y ☰ con correo/tema/estadísticas debajo.

• Solo se tocó la cabecera · 235 tests, lint y build OK
• CI verde · commit ceed26b

Números: 17 regiones · 119 textos · 117 descargables · 117/117 descargas OK · 235 tests.

## Cambios
- `Header.jsx` (único archivo): el contenedor flex (`flex flex-wrap justify-between` con `order`) se sustituye por un **grid con áreas**:
  - Móvil: `[grid-template-areas:'titu_titu'_'menu_bots']` con `grid-cols-[auto_1fr]` → fila 1 = título (ocupa las 2 columnas), fila 2 = hamburguesa (izquierda) + botones (derecha, `justify-self-end`).
  - Web (`md+`): `md:[grid-template-areas:'menu_titu_bots']` con `md:grid-cols-[auto_1fr_auto]` → hamburguesa · título · botones en una fila; el título va en la columna central `1fr` pero con `md:justify-self-start` queda **anclado a la izquierda**, junto a la hamburguesa, y el espacio sobrante empuja los botones a la derecha (sin centrar el título).
  - Cada hijo declara su área (`[grid-area:menu]`, `[grid-area:titu]`, `[grid-area:bots]`).

## Verificación
- `npm run check`: 14 archivos, **235 tests OK**, lint 0 errores, build OK.
- CI de Pages: run 32189461307 **success** (main ceed26b).
- Producción: `https://laidea.antarquia.cl/` 200, bundle `index-9b7ecad9.js`.

## Plan para el próximo día
- Pendientes UX heredados del reporte de navegación (§2.4, cambios 5-6): cross-links Teorías/Rutas/Glosario → Biblioteca con filtros precargados; consolidar labels del nav (quitar `(N)` de Mapa).
- Catálogo: seguir importando obras con `@content-importer` (~400 PDFs disponibles); quedan 2 sin archivo: Regeneración (periódico) y Luis E. Recabarren (copyright LOM).