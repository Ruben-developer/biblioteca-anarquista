# Log del agente — 2026-08-14 00:00

- **Fecha/hora**: 2026-08-14 00:00 (turno cron 00:00)
- **Resultado**: ✅ ÉXITO
- **Rama**: main (commit e5ec0e6)

## Inspección (paso 1.5) — sin errores críticos ni menores
- `npm run check-downloads`: **113/113 OK** (todos los `filename` del catálogo con HTTP 200 en el contenedor).
- Regiones sincronizadas: **17/17/17** (verificado con `vite-node`: `regionData.js` ↔ `countryData.js` ↔ `REGIONS`; 0 regiones sin ISO, 0 ISO sin región; `REGIONS` = 17 + `all` de filtro, intencional).
- Invariante `timeline == mapa`: **44/44** — los 44 textos históricos del catálogo están vinculados a los 32 eventos; 0 títulos fantasma en `relatedTexts`; 0 eventos `con_texto` sin textos.
- `npm audit`: 5 vulnerabilidades **solo en devDeps build-time** (vite/vitest/vite-node/esbuild). El fix exigiría `--force` e instalaría Vite 8 (breaking, rompería el stack Vite 4) → **no aplica a GitHub Pages**. Se documenta, no se fuerza.
- Build sin warnings nuevos (134 warnings de prop-types ya tolerados por convención; 0 errores).

## Tarea del plan (FASE 3, checkbox pendiente "Más filtros y búsqueda avanzada")
El catálogo ya filtraba por búsqueda (título/autor), categoría, región y década.
Se amplió con **búsqueda avanzada** de tres ejes que responden a necesidades reales
del archivo (6 obras sin archivo, textos históricos vs de ideas, favoritos).

### Cambios (commit `e5ec0e6`)
1. **`src/utils/library.js`** — `filterBooks` gana 3 opciones nuevas (todas con
   default compatible → no rompen los usos anteriores):
   - `availability`: `'all'` | `'withFile'` (solo con archivo) | `'withoutFile'` (solo sin archivo).
   - `type`: `'all'` | `'historical'` (categorías del mapa/timeline) | `'ideas'` (teoría/biografía/diálogo).
   - `favorites`: array de títulos guardados como favoritos (`null` desactiva el filtro).
2. **`src/components/LibraryView.jsx`** — 3 selectores nuevos en la fila de filtros:
   - **Disponibilidad**: Con y sin archivo / Solo con archivo / Solo sin archivo.
   - **Tipo de obra**: Todos / Solo históricos / Solo ideas.
   - **Favoritos**: Todas las obras / Solo favoritas.
   - "Limpiar filtros" ahora resetea también los nuevos filtros; el botón aparece
     si cualquiera está activo; texto del contador actualizado para mencionarlos.

### Tests (175 → 182)
- `library.test.js`: 4 tests de `filterBooks` (disponibilidad con/sin archivo,
  tipo históricos vs ideas, favoritos solo cuando se pasa la lista, combinación
  de filtros avanzados con los básicos).
- `LibraryView.test.jsx`: 1 test de los controles avanzados renderizados.
- `Interactions.test.jsx`: 2 tests interactivos jsdom (disponibilidad + tipo con
  grid y estado vacío; favoritas combinadas con búsqueda). Textos del contador
  actualizados en los 3 tests que los verificaban.

## Verificación
- `npm run check`: **lint 0 errores** (134 warnings prop-types tolerados) + **182 tests OK** + build de producción OK.
- CI de Pages: **verde** (run 31768723164, `gh run watch --exit-status` OK).

## Métricas del día (registradas en `data/registros/registro.json`)
- Obras en catálogo: 119 (44 históricos / 75 ideas) · 17 regiones · 32 eventos · 113 descargables.
- Tests: 175 → **182**.
- Commits del turno: 1 (e5ec0e6).
- CI: verde.

## Próximo día (sugerido)
1. **FASE 3 (siguiente checkbox pendiente)**: "Mapas visuales por región".
2. Alternativa de contenido: ampliar el catálogo con `@content-importer`
   (~400 PDFs del contenedor aún disponibles; `sin_clasificar/` está vacío,
   habría que seleccionar lote de `anarquismo/` no importado).
3. Idea ligada a lo de hoy: filtro por **autor** como selector dedicado en la
   Biblioteca (la búsqueda ya cubre autor, pero un selector facilitaría explorar).
4. Pendientes conocidos: 6 libros sin `filename` (esperan fuentes para `@content-importer`);
   `@ux-review` pendiente de re-invocación (último reporte 2026-08-11).
