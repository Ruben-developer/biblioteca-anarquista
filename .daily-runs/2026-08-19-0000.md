# Log del turno — 2026-08-19 00:00 (daily-dev)

**Resultado: ✅ ÉXITO** (verificación completa verde + CI verde)

## Notificación Telegram
🌍 La Idea — cross-links al catálogo + nav sin contador

Los menús de Teorías, Rutas y Glosario ahora enlazan con la Biblioteca: cada obra tiene un botón "En el catálogo" que abre el catálogo buscando ese título, y al pie de cada vista queda "Ver todas las obras del catálogo". Al navegar a Biblioteca desde el menú se limpian los filtros previos (nada de filtros "pegajosos").

• 🔗 Botón "En el catálogo" por obra en Teorías, Rutas y Glosario (icono Library)
• 🗂️ Biblioteca precargada con filtros (`initialFilters`) + botón global al pie
• 🧭 El label del nav "Mapa (17)" pasa a "Mapa" (el contador ya se ve en la vista)
• ✅ Tests 235 → 246 · lint 0 errores · build OK · descargas 117/117

Números: 17 regiones · 119 textos · 44 históricos · 117/117 descargas OK · 246 tests.

Commits 63c7e2e + 6622ba4 pusheados · CI verde.

---

## 1. Inspección (paso 1.5)
- `npm run check-downloads`: **117/117 OK**.
- Regiones sincronizadas **17/17/17** (`regionData.js` fuente única; 0 regiones sin ISO, 0 ISO sin región, `REGIONS` derivado).
- Invariante `timeline == mapa`: **44/44** textos históricos vinculados, 0 títulos fantasma, 0 `con_texto` sin textos (verificado con vite-node contra el catálogo completo).
- `npm audit`: 5 vulnerabilidades SOLO en devDeps build-time (vite/vitest/vite-node/esbuild). El fix exigiría `--force` y rompería Vite 4 → no aplica a Pages. Sin cambios.
- Build sin warnings nuevos.
- **Sin errores críticos** → se continúa con la siguiente tarea del plan.

## 2. Tarea del plan
Cambios 5-6 del reporte `@ux-review` de navegación (2026-08-17, §2.4):
- **`LibraryView` gana `initialFilters`** (searchTerm, categoría, región, década, autor, disponibilidad, tipo, favoritos): la Biblioteca nace con esos filtros aplicados (`seed` en useState) y un `useEffect` resincroniza si el cross-link cambia sin desmontar la vista. `DEFAULT_FILTERS` centraliza también "Limpiar filtros".
- **`AnarchistArchive`**: estado `libraryInitialFilters` + `openLibraryWithFilters(filters)`; `handleViewChange` limpia los filtros al navegar a Biblioteca por el nav (menú = catálogo completo, sin filtros pegajosos).
- **`TheoriesView` / `ReadingPathsView` / `GlossaryView`**: botón **"En el catálogo"** por obra (`onOpenLibrary({ searchTerm: book.title })`, icono `Library`) y botón global **"Ver todas las obras del catálogo"** al pie (`onOpenLibrary({})`).
- **`Navigation`**: label de Mapa `Mapa (N)` → `Mapa`; eliminada la prop `regionCount` (ya no se usa).

## 3. Verificación
- `npm run check`: **lint 0 errores + 246 tests (235 → 246) + build OK**.
- `npm run check-downloads`: **117/117 OK**.
- CI de Pages: **verde** (run 32214940355).

## 4. Commits
- `63c7e2e` — `feat(ux): cross-links Teorías/Rutas/Glosario -> Biblioteca con filtros precargados y nav sin (N) en Mapa`
- `6622ba4` — `test(ux): cobertura de cross-links a Biblioteca y label de Mapa sin contador`

## 5. Métricas
- `data/registros/registro.json`: entrada nueva añadida (obras 119, palabras 3876, commits 2, PRs 0, CI verde). 42 → 43 entradas.

## 6. Plan para el próximo día
- **UX/UI semanal**: invocar a `@ux-review` (última revisión 2026-08-17) y aplicar hallazgos priorizados (backlog §5.2: estética/paleta/tipografía).
- **Recabarren / Regeneración**: buscar fuentes libres (Regeneración como antología magonista; Recabarren en Dialnet/FECH) o dejar documentado.
- **aria-pressed** en toggle de tema y corazones de favoritos (pendiente UX reporte navegación).
- FASE 5: ampliar el catálogo con `@content-importer` (~400 PDFs disponibles).