# Log diario — 2026-08-17 00:00 (turno 00:00)

**Resultado: ÉXITO** — tarea del plan completada, build/tests/lint verdes, CI verde.

## Notificación Telegram
🌍 Biblioteca Anarquista — vista agrupada por autor en la Biblioteca

Nueva forma de explorar el catálogo: un botón "Agrupar por autor" en la Biblioteca
junta en una sola tarjeta todas las obras de cada autor, con su número de obras
y cada obra en una fila compacta (región, año, categoría, favorito, evento y Leer).

• 🛠️ Nueva util `groupBooksByAuthor`: agrupa por autor (sin distinguir mayúsculas),
  ordena de más a menos obras y las obras sin autor van a "Anónimo"
• 🛠️ El botón alterna entre el grid de tarjetas y la vista agrupada, y respeta
  todos los filtros activos (búsqueda, autor, década, disponibilidad…)
• ✅ Tests: 185 → 192 · lint 0 errores · build OK

Números: 17 regiones · 119 textos · 44 históricos · 113/113 descargas OK · 192 tests.

Commit b57d41f pusheado · CI verde.

## Inspección (paso 1.5)
- Descargas: `npm run check-downloads` → **113/113 OK**.
- Regiones sincronizadas: **17/17/17** (`regionData.js` ↔ `COUNTRY_ISO` ↔ `REGIONS`),
  verificadas con `vite-node` (0 regiones sin ISO, 0 ISO sin región).
- Invariante `timeline == mapa`: **44/44** — 32 eventos, 0 títulos fantasma
  (los `relatedTexts` apuntan siempre a libros reales del catálogo), 0 `con_texto`
  sin textos.
- `npm audit`: 5 vulnerabilidades SOLO en devDeps build-time
  (vite/vitest/vite-node/esbuild); el fix exigiría `--force` y rompería Vite 4 →
  no aplica a Pages. Conocido, sin acción.
- Build sin warnings. Sin errores críticos → se continuó con la siguiente tarea.

## Tarea del plan
"Próximo día" de 2026-08-16 (FASE 3): **vista agrupada por autor en la Biblioteca**
(el plan sugería agrupar las obras de un autor; el selector dedicado de ayer ya
las juntaba al filtrar, pero una vista agrupada lo hace directo sin filtrar).

### Cambios (commit b57d41f)
- `src/utils/library.js`: nueva util `groupBooksByAuthor(books)` — agrupa una
  lista plana de libros por autor (nombre normalizado: trim + sin distinción de
  mayúsculas), ordena los grupos de más a menos obras (alfabético en empate) y
  coloca las obras sin autor bajo "Anónimo". Devuelve `[]` sin datos.
- `src/components/LibraryView.jsx`: botón "Agrupar por autor" (`aria-pressed`,
  icono `Users`) que alterna entre el grid de tarjetas individuales y una
  **tarjeta por autor** con su número de obras ("N obras") y las obras en filas
  compactas (región, año, categoría, rating, corazón de favorito, enlace "Ver en
  la línea temporal" y botón Leer / "Sin archivo disponible"). Opera sobre los
  libros YA filtrados y ordenados, así que respeta búsqueda, autor, década,
  disponibilidad, tipo y favoritos.
- Tests: 185 → **192** (4 de `groupBooksByAuthor` en `library.test.js`:
  agrupación y orden, normalización + Anónimo, empate alfabético, lista
  vacía/null; 1 estático en `LibraryView.test.jsx` con el botón renderizado;
  2 interactivos en `Interactions.test.jsx`: alternar agrupar/desagrupar sobre
  el grid y vista agrupada que respeta el filtro de autor).

## Verificación
- `npm run check`: lint 0 errores + **192 tests** + build OK.
- CI de Pages: **verde** (run 31993262865, conclusion success).
  https://github.com/Ruben-developer/biblioteca-anarquista/actions/runs/31993262865

## Métricas (registro.json)
- Obras: 119 · Palabras catálogo: 4042 · Commits del día: 1 · PRs: 0 · CI: verde.

## Plan para el próximo día
- Siguiente tarea sugerida (FASE 3 pendiente): "Mapas visuales por región", o
  ampliar el catálogo con `@content-importer` (~400 PDFs del contenedor aún
  disponibles), o una vista agrupada por **región** (análoga a la de autor de hoy).