# Log 2026-08-18 17:29 — Título visible en móvil + fuera píldoras horizontales

Resultado: **éxito** — 235 tests, lint 0 errores, build OK, CI verde.

## Notificación Telegram
🌍 La Idea — título en móvil y menú unificado

Dos retoques de navegación que pediste:
• 📱 En smartphone el título «La Idea» vuelve a verse: la cabecera se apila en dos filas — arriba ☰ Biblioteca junto a correo/tema/estadísticas, y debajo el título.
• 🖥️ En web se quitaron las píldoras horizontales del menú: toda la navegación pasa por la hamburguesa ☰, que queda como icono a la izquierda del título y abre el panel lateral igual que en el móvil.

• Tests actualizados al nuevo flujo (el menú se abre por la hamburguesa): 234 → 235, lint y build OK
• CI verde · commit 32f35cf

Números: 17 regiones · 119 textos · 117 descargables · 117/117 descargas OK · 235 tests.

## Cambios
- `Header.jsx`: el bloque del título deja de ocultarse en móvil (`hidden md:block` eliminado). Con `flex-wrap` + `order` + `w-full` el header se apila en móvil: fila 1 = hamburguesa (con texto del menú activo) + correo/tema/estadísticas; fila 2 = «🏴 La Idea» a ancho completo. En `md+`: hamburguesa (order-1) + título (order-2) + botones (order-3) en una sola fila.
- `Navigation.jsx`: se elimina el bloque de píldoras horizontales (`hidden md:flex` con `overflow-x-auto`) y el `<nav aria-label="Navegación principal">` exterior; el componente queda solo con el drawer lateral (todos los tamaños), controlado por `menuOpen`/`onMenuClose` desde el padre.
- `AnarchistArchive.test.jsx`: el test estático ya no espera los destinos del nav en el DOM (el drawer está cerrado); los tests interactivos abren el drawer con la hamburguesa antes de navegar (helper `openDrawer`).
- `Views.test.jsx`: describe Navigation renderiza con `menuOpen` para comprobar el contenido del drawer, y añade casos drawer cerrado (no renderiza dialog).
- `Interactions.test.jsx`: describe "navegación completa" abre el drawer antes de cada clic; el test del drawer ya no espera píldoras en el DOM (se verifica su ausencia); el test de estadísticas valida la vista inicial por el buscador en vez de por un botón de píldora.

## Verificación
- `npm run check`: 14 archivos, **235 tests OK** (antes 234), lint 0 errores, build OK (dist/assets/index-88806d49.js).
- CI de Pages: run 32187666117 **success** (main 32f35cf).
- Producción: `https://laidea.antarquia.cl/` 200, bundle `index-e0c4d69b.js` con los nuevos strings ("Abrir menú de navegación", "Navegación lateral") confirmados en el bundle desplegado.

## Plan para el próximo día
- Pendientes UX heredados del reporte de navegación (§2.4, cambios 5-6): cross-links Teorías/Rutas/Glosario → Biblioteca con filtros precargados; consolidar labels del nav (quitar `(N)` de Mapa).
- Catálogo: seguir importando obras con `@content-importer` (~400 PDFs disponibles); quedan 2 sin archivo: Regeneración (periódico) y Luis E. Recabarren (copyright LOM).
- Ampliar el catálogo hasta agotar los PDFs del contenedor (FASE 5).